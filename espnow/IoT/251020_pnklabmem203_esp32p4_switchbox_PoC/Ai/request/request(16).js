// request(16).js
window.requestData = window.requestData || [];
window.requestData[16] = {
  "request_number": 16,
  "title": "프레임 드롭 및 배치 버퍼 할당 실패 해결",
  "date": "2025-10-21",
  "author": "Claude (AI Assistant)",
  "status": "completed",
  "tags": [
    "request"
  ],
  "problem": {
    "summary": "### 1.1 실행 로그 분석"
  },
  "solution": {
    "summary": "### 2.1 메모리 할당 실패"
  },
  "raw_content": {
    "question": "심각한 문제 발생\n\n### 1.1 실행 로그 분석\n\n**배치 버퍼 할당 실패**:\n```\nI (2386) mjpeg_spool: Hybrid spool initialized: ring buffer 8 frames (0.27 sec), SD streaming to /sdcard/TEMP_RAW.yuv\nW (2386) mjpeg_spool: SPIRAM allocation failed, trying internal RAM for batch buffer\nE (2403) mjpeg_spool: Writer task: failed to allocate batch buffer (3037 KB)\n```\n\n**프레임 대량 드롭**:\n```\nW (3447) mjpeg_spool: Ring buffer full, dropped 10 frames\nW (3780) mjpeg_spool: Ring buffer full, dropped 20 frames\n...\nW (12112) mjpeg_spool: Ring buffer full, dropped 270 frames\n```\n\n**최종 결과**:\n```\nI (12412) app_main: 총 프레임: 7개, 실제 FPS: 0.70\n```\n→ 10초 동영상에서 300 프레임 예상, 7 프레임만 저장됨 (97.7% 손실!)\n\n**AVI 저장 멈춤**:\n```\nI (12423) app_main: 동영상 녹화 후 저장중 0%\n(무한 대기...)\n```\n\n### 1.2 문제 원인 분석\n\n**1. Writer Task 시작 실패**:\n```\n배치 버퍼 크기: 1 프레임 = 2.97 MB = 3037 KB\nSPIRAM 할당: 실패 (전체가 링 버퍼에 사용됨)\n내부 RAM 할당: 실패 (약 545 KB만 사용 가능, 3037 KB 필요)\n```\n\n**2. SD 카드 쓰기 불가능**:\n- Writer task가 배치 버퍼 할당 실패로 종료됨\n- 링 버퍼만 계속 쌓임\n- SD 카드에 쓰기 못함\n\n**3. 링 버퍼 포화**:\n- 링 버퍼: 8 프레임 (0.27초)\n- 카메라: 30 fps로 계속 캡쳐\n- Writer: 동작 안 함\n- 결과: 0.27초마다 8 프레임씩 드롭\n\n**4. AVI 저장 멈춤**:\n- TEMP_RAW.yuv 파일에 7 프레임만 저장됨\n- AVI 변환 중 파일 읽기 또는 처리 오류\n- 원인: 매우 적은 프레임 수 또는 파일 손상\n\n",
    "answer": "근본 원인\n\n### 2.1 메모리 할당 실패\n\n**내부 RAM 제약**:\n```\nESP32-P4 내부 RAM:\n- 총 메모리: ~545 KB (heap_init 로그 참조)\n  - At 4FF170D0: 143 KiB\n  - At 4FF3AFC0: 18 KiB\n  - At 4FF40000: 384 KiB\n  - At 50108080: 31 KiB (RTCRAM)\n  - At 30100068: 7 KiB (TCM)\n\n배치 버퍼 필요: 3037 KB (1 프레임)\n→ 내부 RAM 총량의 558% ❌\n```\n\n**SPIRAM 상황**:\n```\n총 SPIRAM: 32 MB\n시스템 예약: ~8 MB\n사용 가능: 24.04 MB\n링 버퍼 사용: 23.73 MB (8 프레임)\n남은 SPIRAM: 0.31 MB (310 KB)\n배치 버퍼 필요: 2.97 MB\n→ 할당 불가능 ❌\n```\n\n### 2.2 설계 결함\n\n**이전 설계 (잘못됨)**:\n```\n1. 링 버퍼: SPIRAM에 할당\n2. 배치 버퍼: 별도 메모리 할당 (SPIRAM → 실패 시 내부 RAM)\n3. Writer task: 링 버퍼에서 배치 버퍼로 복사 → SD 카드 쓰기\n```\n\n**문제점**:\n- ❌ 중복 메모리 할당 (링 + 배치)\n- ❌ 내부 RAM은 프레임 버퍼에 충분하지 않음\n- ❌ 배치 버퍼 할당 실패 시 writer 완전 중단\n\n\n\n해결 방법\n\n### 3.1 배치 버퍼 완전 제거\n\n**새로운 설계**:\n```\n1. 링 버퍼: SPIRAM에 할당 (유지)\n2. 배치 버퍼: 제거 ✅\n3. Writer task: 링 버퍼에서 직접 SD 카드 쓰기 ✅\n```\n\n**장점**:\n- ✅ 추가 메모리 할당 불필요\n- ✅ Writer task 시작 보장\n- ✅ 메모리 복사 오버헤드 제거\n- ✅ 단순한 구조\n\n**단점**:\n- ⚠️ SD 카드 쓰기 효율 약간 감소 (배치 쓰기 → 단일 프레임 쓰기)\n- ⚠️ Mutex 보유 시간 약간 증가 (포인터만 가져오므로 미미)\n\n### 3.2 코드 수정\n\n**파일**: [main/mjpeg_spool.c](../main/mjpeg_spool.c)\n\n#### 수정 1: Writer Task 초기화 (라인 58-59)\n\n**수정 전**:\n```c\n// Use minimal batch size (1 frame) to save SPIRAM memory\nconst uint32_t batch_size = 1;\nsize_t batch_buf_size = s->frame_size * batch_size;\n\n// Try SPIRAM first, then internal RAM\nuint8_t *batch_buf = heap_caps_malloc(batch_buf_size, MALLOC_CAP_SPIRAM);\nif (!batch_buf) {\n    ESP_LOGW(TAG, \"SPIRAM allocation failed, trying internal RAM for batch buffer\");\n    batch_buf = malloc(batch_buf_size);\n}\n\nif (!batch_buf) {\n    ESP_LOGE(TAG, \"Writer task: failed to allocate batch buffer (%zu KB)\", batch_buf_size / 1024);\n    vTaskDelete(NULL);\n    return;\n}\n\nESP_LOGI(TAG, \"SD writer task started (batch write: %\"PRIu32\" frames)\", batch_size);\n```\n\n**수정 후**:\n```c\n// Write directly from ring buffer (no batch buffer needed)\nESP_LOGI(TAG, \"SD writer task started (direct write from ring buffer)\");\n```\n\n**변경 사항**:\n- ✅ 배치 버퍼 할당 코드 완전 제거\n- ✅ 에러 처리 제거 (할당 실패 불가능)\n- ✅ 코드 간소화\n\n#### 수정 2: SD 쓰기 로직 (라인 72-86)\n\n**수정 전**:\n```c\n// Calculate how many frames available (inside mutex)\nxSemaphoreTake(s->mutex, portMAX_DELAY);\nuint32_t available;\nif (s->write_idx >= s->read_idx) {\n    available = s->write_idx - s->read_idx;\n} else {\n    available = s->cap - s->read_idx + s->write_idx;\n}\n\n// Write in batches for better SD performance\nuint32_t to_write = (available > batch_size) ? batch_size : available;\n\n// Copy batch from ring buffer (inside mutex - fast memcpy)\nfor (uint32_t i = 0; i < to_write; i++) {\n    uint32_t idx = (s->read_idx + i) % s->cap;\n    uint8_t *src = s->ring_buffer + (idx * s->frame_size);\n    memcpy(batch_buf + (i * s->frame_size), src, s->frame_size);\n}\n\ns->read_idx = (s->read_idx + to_write) % s->cap;\nxSemaphoreGive(s->mutex);  // Release mutex BEFORE SD write\n\n// Write entire batch to SD card (OUTSIDE mutex - no logging during write)\nsize_t total_size = to_write * s->frame_size;\nsize_t written = fwrite(batch_buf, 1, total_size, s->raw_fp);\n\nif (written == total_size) {\n    s->frames_written += to_write;\n}\n```\n\n**수정 후**:\n```c\n// Write one frame at a time directly from ring buffer\nxSemaphoreTake(s->mutex, portMAX_DELAY);\n\n// Get pointer to frame in ring buffer\nuint8_t *frame_ptr = s->ring_buffer + (s->read_idx * s->frame_size);\ns->read_idx = (s->read_idx + 1) % s->cap;\n\nxSemaphoreGive(s->mutex);  // Release mutex BEFORE SD write\n\n// Write frame directly to SD card\nsize_t written = fwrite(frame_ptr, 1, s->frame_size, s->raw_fp);\n\nif (written == s->frame_size) {\n    s->frames_written++;\n}\n```\n\n**변경 사항**:\n- ✅ 배치 복사 제거 (memcpy 불필요)\n- ✅ 포인터만 가져오기 (Mutex 보유 시간 최소화)\n- ✅ 직접 쓰기 (fwrite 1회로 간소화)\n- ✅ 프레임 단위 처리\n\n#### 수정 3: 정리 코드 (라인 101-107)\n\n**수정 전**:\n```c\n// Notify waiter (if any) that writer is done and file is closed\nif (s->flush_waiter) {\n    xTaskNotifyGive(s->flush_waiter);\n    s->flush_waiter = NULL;\n}\n\n// Free batch buffer (could be from SPIRAM or internal RAM)\nfree(batch_buf);\nvTaskDelete(NULL);\n```\n\n**수정 후**:\n```c\n// Notify waiter (if any) that writer is done and file is closed\nif (s->flush_waiter) {\n    xTaskNotifyGive(s->flush_waiter);\n    s->flush_waiter = NULL;\n}\n\nvTaskDelete(NULL);\n```\n\n**변경 사항**:\n- ✅ 배치 버퍼 해제 코드 제거\n\n\n\n예상 개선 효과\n\n### 4.1 메모리 사용\n\n**이전** (배치 버퍼 사용):\n```\nSPIRAM:\n- 링 버퍼: 23.73 MB (8 프레임)\n- 배치 버퍼: 할당 시도 실패 → Writer 종료\n\n내부 RAM:\n- 배치 버퍼: 3037 KB 할당 시도 실패\n\n결과: Writer task 동작 불가 ❌\n```\n\n**이후** (배치 버퍼 제거):\n```\nSPIRAM:\n- 링 버퍼: 23.73 MB (8 프레임)\n\n내부 RAM:\n- 추가 할당 없음\n\n결과: Writer task 정상 동작 예상 ✅\n```\n\n### 4.2 성능\n\n**Writer Task 동작 보장**:\n- ✅ 메모리 할당 실패 불가능\n- ✅ Task 시작 100% 보장\n- ✅ SD 카드 쓰기 계속 진행\n\n**프레임 드롭 제거**:\n```\n이전: 270+ 프레임 드롭 (97.7% 손실)\n예상: 0-5 프레임 드롭 (SD 카드 성능 의존)\n```\n\n**FPS 개선**:\n```\n이전: 0.70 fps (7 프레임 / 10초)\n예상: 30 fps (300 프레임 / 10초)\n```\n\n### 4.3 SD 쓰기 성능\n\n**배치 쓰기 vs 단일 쓰기**:\n```\n배치 쓰기 (이전):\n- 쓰기 횟수: 300 프레임 / 1 프레임 = 300회\n- 쓰기 크기: 2.97 MB × 1 = 2.97 MB\n- 총 오버헤드: ~300 × 1ms = 300ms\n\n단일 쓰기 (이후):\n- 쓰기 횟수: 300 프레임 / 1 프레임 = 300회 (동일)\n- 쓰기 크기: 2.97 MB\n- 총 오버헤드: ~300 × 1ms = 300ms (동일)\n\n결론: 배치 크기가 1이므로 성능 차이 없음 ✅\n```\n\n**SD 카드 레이턴시**:\n```\n프레임 크기: 2.97 MB\n필요 쓰기 속도: 2.97 MB × 30 fps = 89.1 MB/s\nSD 카드 성능: 40 MHz = 40 MB/s (로그 참조)\n\n문제: 40 MB/s < 89.1 MB/s ❌\n결과: 여전히 프레임 드롭 가능성 있음\n```\n\n\n\n남은 문제\n\n### 5.1 SD 카드 성능 부족\n\n**현재 SD 카드**:\n```\nName: SC16G\nType: SDHC\nSpeed: 40.00 MHz (limit: 40.00 MHz) → 약 40 MB/s\n```\n\n**필요 성능**:\n```\n데이터 레이트: 89.1 MB/s\n현재 성능: 40 MB/s\n부족: 49.1 MB/s (122% 추가 필요)\n```\n\n**해결 방법**:\n1. **SD 카드 업그레이드**: UHS-I U3 (최소 90 MB/s)\n2. **해상도 낮추기**: 1280x960 (40% 메모리 절감)\n3. **프레임레이트 낮추기**: 20 fps (필요 속도: 59.4 MB/s)\n\n### 5.2 AVI 저장 멈춤\n\n**원인 추정**:\n1. 매우 적은 프레임 수 (7 프레임)\n2. 파일 손상 또는 불완전한 데이터\n3. AVI 변환 로직 버그 (0% 진행률에서 정지)\n\n**확인 필요**:\n- TEMP_RAW.yuv 파일 크기\n- AVI 변환 코드의 에러 처리\n- 메모리 부족 여부\n\n\n\n테스트 시나리오\n\n### 6.1 성공 기준\n\n**Writer Task 시작**:\n```\nI (xxxx) mjpeg_spool: SD writer task started (direct write from ring buffer)\n```\n\n**프레임 저장**:\n```\nI (xxxx+10s) app_main: 총 프레임: 290-300개, 실제 FPS: 29-30\n```\n\n**프레임 드롭**:\n```\nW (xxxx) mjpeg_spool: Ring buffer full, dropped 0-10 frames\n(또는 드롭 없음)\n```\n\n**AVI 저장**:\n```\nI (xxxx) app_main: 동영상 녹화 후 저장중 0%\nI (xxxx+5s) app_main: 동영상 녹화 후 저장중 50%\nI (xxxx+10s) app_main: 동영상 녹화 후 저장중 100%\nI (xxxx+10s) app_main: 동영상 파일 저장 완료: /sdcard/VIDEO001.AVI\n```\n\n### 6.2 실패 시나리오\n\n**여전히 프레임 드롭**:\n```\nW (xxxx) mjpeg_spool: Ring buffer full, dropped 100+ frames\n```\n→ SD 카드 성능 부족\n→ 해상도 또는 프레임레이트 낮춰야 함\n\n**Writer Task 멈춤**:\n```\nI (xxxx) mjpeg_spool: SD writer task started (direct write from ring buffer)\n(그 후 아무 로그 없음)\n```\n→ SD 카드 쓰기 오류\n→ 파일 시스템 문제\n\n\n\n추가 최적화 권장\n\n### 7.1 SD 카드 쓰기 버퍼링\n\n**fwrite 버퍼 크기 증가**:\n```c\n// mjpeg_spool_start() 내부\nsetvbuf(s->raw_fp, NULL, _IOFBF, 64 * 1024);  // 64 KB 버퍼\n```\n→ fwrite 호출 횟수 감소\n\n### 7.2 프레임레이트 동적 조정\n\n**프레임 드롭 감지 시 프레임레이트 낮추기**:\n```c\nstatic uint32_t consecutive_drops = 0;\nif (s->read_idx == ((s->write_idx + 1) % s->cap)) {\n    consecutive_drops++;\n    if (consecutive_drops > 10) {\n        // 프레임레이트 낮추기 (30 fps → 20 fps)\n        ESP_LOGW(TAG, \"Too many drops, reducing frame rate\");\n    }\n} else {\n    consecutive_drops = 0;\n}\n```\n\n### 7.3 해상도 옵션 추가\n\n**sdkconfig.defaults에 여러 해상도 준비**:\n```ini\n# Option 1: 1080p @ 30fps (current, 89.1 MB/s)\nCONFIG_CAMERA_OV5647_MIPI_RAW10_1920x1080_30FPS=y\n\n# Option 2: 960p @ 45fps (59.4 MB/s)\n# CONFIG_CAMERA_OV5647_MIPI_RAW10_1280x960_BINNING_45FPS=y\n\n# Option 3: 640p @ 50fps (40 MB/s)\n# CONFIG_CAMERA_OV5647_MIPI_RAW8_800x640_50FPS=y\n```\n\n\n\n결론\n\n### 8.1 핵심 해결책\n\n**배치 버퍼 완전 제거**:\n- ✅ 메모리 할당 실패 제거\n- ✅ Writer task 동작 보장\n- ✅ 코드 간소화\n- ✅ 프레임 드롭 대폭 감소 예상\n\n**직접 쓰기 방식**:\n- ✅ 링 버퍼에서 직접 SD 카드로 쓰기\n- ✅ 추가 메모리 불필요\n- ✅ 복사 오버헤드 제거\n\n### 8.2 성능 예측\n\n**Writer Task**:\n- 이전: 시작 실패 ❌\n- 이후: 정상 동작 ✅\n\n**프레임 저장**:\n- 이전: 7 / 300 프레임 (2.3%)\n- 예상: 270-300 / 300 프레임 (90-100%)\n\n**SD 카드 병목**:\n- ⚠️ 40 MB/s < 89.1 MB/s 필요\n- ⚠️ 여전히 일부 프레임 드롭 가능\n- 해결: SD 카드 업그레이드 또는 해상도 조정\n\n### 8.3 다음 단계\n\n**즉시 테스트**:\n1. 빌드 및 플래시\n2. Writer task 시작 확인\n3. 10초 녹화 후 프레임 수 확인\n4. 프레임 드롭 로그 확인\n\n**SD 카드 성능 부족 시**:\n1. 더 빠른 SD 카드 사용 (UHS-I U3)\n2. 해상도를 1280x960으로 낮추기\n3. 프레임레이트를 20 fps로 낮추기\n\n**AVI 저장 문제 해결**:\n1. TEMP_RAW.yuv 파일 크기 확인\n2. AVI 변환 로직 디버깅\n3. 메모리 부족 여부 확인\n\n===========================================\n작업 완료 - AI 어시스턴트 Claude\n===========================================\n"
  },
  "sections": {
    "심각한 문제 발생": "### 1.1 실행 로그 분석",
    "근본 원인": "### 2.1 메모리 할당 실패",
    "해결 방법": "### 3.1 배치 버퍼 완전 제거",
    "예상 개선 효과": "### 4.1 메모리 사용",
    "남은 문제": "### 5.1 SD 카드 성능 부족",
    "테스트 시나리오": "### 6.1 성공 기준",
    "추가 최적화 권장": "### 7.1 SD 카드 쓰기 버퍼링",
    "결론": "### 8.1 핵심 해결책"
  }
};
